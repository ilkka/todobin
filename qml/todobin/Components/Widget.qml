import QtQuick 1.0

Item {
    id: widgetroot

    // Margin between component edge and background
    property int margin: 5

    // Whether or not widget is in enabled state
    property bool enabled: true
}
